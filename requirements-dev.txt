# Development / CI only - not installed in the runtime image.
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
#   .venv/bin/python -m pytest
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
# Parallel workers and a per-test hang timeout - both configured in pytest.ini
# (see the "Tests" section of the README). Without them the suite still runs,
# just serially and without the deadlock safety net.
pytest-xdist>=3.5
pytest-timeout>=2.3
