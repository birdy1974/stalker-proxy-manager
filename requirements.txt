# Stalker Proxy Manager - Python dependencies
# Pinned versions are validated in the GitHub build workflow.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29            # Postgres driver (production)
aiosqlite>=0.20          # SQLite driver (dev/fallback)
httpx>=0.27              # async HTTP client for Stalker portal calls
jinja2>=3.1              # server-side templates (GUI)
python-multipart>=0.0.9  # form parsing (login, imports)
itsdangerous>=2.2        # signed session cookies (admin auth)
